# Upgrade and Rollback

Back up first, stop Seymour, install the new release, apply migrations once, start API then Stratum, and verify. Keep the prior release and database backup until acceptance completes.
