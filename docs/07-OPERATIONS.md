# Operations

Use `systemctl status`, `journalctl -u seymour-pool-engine-api -f`, `journalctl -u seymour-pool-engine-stratum -f`, and `scripts/production/health-check.sh`. Investigate recurring failures before repeated restarts.
