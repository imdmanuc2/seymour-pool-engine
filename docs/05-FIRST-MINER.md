# First Miner

Set one miner primary to `stratum+tcp://SEYMOUR_HOST:3334`, worker name as username, and `x` as password. Keep CKPool as backup. Confirm authorization, jobs, accepted shares, VarDiff, and stable hashrate.
