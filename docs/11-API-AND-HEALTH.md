# API and Health

API defaults to port 8561. OpenAPI is `/docs`; health is `/api/v1/health`. Other routes expose templates, Stratum, candidates, VarDiff, node failover, diagnostics, and acceptance runs.
