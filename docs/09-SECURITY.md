# Security

Use dedicated accounts, strong unique secrets, firewalls, SSH keys, and restrictive permissions. Do not expose Bitcoin RPC or PostgreSQL publicly. Protect the API behind a trusted network or secured reverse proxy.
