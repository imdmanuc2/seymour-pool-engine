# Package 023 — Variable Difficulty & Scaling

Adds adaptive per-session Stratum difficulty using an EWMA share interval, bounded and damped retargeting, persisted difficulty history, session performance fields, and VarDiff status/history APIs.

## Install

```bash
chmod +x scripts/*.sh
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
