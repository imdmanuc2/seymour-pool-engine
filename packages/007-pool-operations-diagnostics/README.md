# Package 007 — Pool Operations & Diagnostics

Adds persisted diagnostic runs, readiness scoring, dependency checks, and operations API routes.

Run `scripts/doctor.sh`, `scripts/install.sh`, then `scripts/verify.sh`.
