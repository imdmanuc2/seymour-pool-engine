# Package 006 — Reward & Accounting Foundation

Adds deterministic proportional reward allocation, immutable balance-ledger entries,
immature-to-confirmed balance transitions, reward periods, worker balances, REST APIs,
and tests.

## Install

```bash
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
