# Package 005 — Block Lifecycle

Adds provider-independent block candidate persistence, lifecycle transitions, confirmation/orphan tracking, immutable block events, block statistics, REST APIs, and deterministic block fixtures.

Run:

```bash
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
