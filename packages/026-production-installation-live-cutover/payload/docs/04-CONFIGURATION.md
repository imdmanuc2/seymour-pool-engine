# Configuration

Use `.env.production.example`. Required values include the database URL, Bitcoin RPC credentials, and a real payout script. Never deploy with payout script `51` or `CHANGE_ME`. API defaults to 8561 and parallel Stratum to 3334.
