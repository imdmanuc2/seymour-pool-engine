# Bitcoin Core

Enable authenticated JSON-RPC with `server=1`, a dedicated RPC user/password, `rpcallowip` for the Seymour host, and a trusted `rpcbind`. Seymour requires `getblocktemplate` and `submitblock`. Never expose port 8332 publicly.
