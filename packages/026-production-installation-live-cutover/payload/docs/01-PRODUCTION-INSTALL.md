# Production Installation

Install Python 3.11+, venv, PostgreSQL client, rsync, and curl. Run `sudo scripts/production/install-production.sh`, edit `/etc/seymour-pool-engine/engine.env`, run preflight, then enable services. The installer does not stop CKPool.
