# Backup and Restore

Back up PostgreSQL, `/etc/seymour-pool-engine/engine.env`, and the deployed release. Protect credentials. Restore the matching release and database, verify health, then reconnect miners.
